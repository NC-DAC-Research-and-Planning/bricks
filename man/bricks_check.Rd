% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{bricks_check}
\alias{bricks_check}
\title{Diagnose a bricks backend's configuration}
\usage{
bricks_check(backend = bricks_backend())
}
\arguments{
\item{backend}{Backend to check. One of \code{"odbc"}, \code{"databricks"}, or
\code{"databricks_connect"}. Defaults to \code{\link[=bricks_backend]{bricks_backend()}}.}
}
\value{
A tibble with one row per check: the \code{check} name, a logical \code{ok},
and a human-readable \code{detail}. Returned invisibly; also printed as a
readable report when called interactively.
}
\description{
Reports, without opening a connection, whether the pieces a backend needs
are in place: the relevant environment variables, the odbc driver, and (for
the Spark backends) a local Spark install and the Windows runtime caveat.
Use it to debug a failing \code{\link[=bricks_connect]{bricks_connect()}} before chasing a connector's
own error message.
}
\examples{
\dontrun{
bricks_check("odbc")
}
}
\seealso{
\code{\link[=bricks_connect]{bricks_connect()}}, \code{\link[=bricks_backend]{bricks_backend()}}
}
