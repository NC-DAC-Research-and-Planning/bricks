% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{databricks_spark_connect}
\alias{databricks_spark_connect}
\title{Connect to Databricks with sparklyr}
\usage{
databricks_spark_connect(
  method = c("databricks_connect", "databricks"),
  cluster_id = Sys.getenv("DATABRICKS_CLUSTER_ID"),
  serverless = !nzchar(cluster_id),
  version = Sys.getenv("DATABRICKS_DBR_VERSION")
)
}
\arguments{
\item{method}{\code{"databricks_connect"} (remote compute from an IDE) or
\code{"databricks"} (native, inside a Databricks notebook).}

\item{cluster_id}{Databricks cluster id. Defaults to the
\code{DATABRICKS_CLUSTER_ID} environment variable.}

\item{serverless}{Use serverless compute. Defaults to \code{TRUE} when no
\code{cluster_id} is available.}

\item{version}{Optional Databricks Runtime version for serverless. Defaults
to the \code{DATABRICKS_DBR_VERSION} environment variable when set.}
}
\value{
A \code{sparklyr} Spark connection.
}
\description{
Opens a \code{sparklyr} Spark connection, either native (inside a Databricks
notebook) or via Databricks Connect / Spark Connect from an IDE. Computation
stays in Spark until you \code{\link[=bricks_collect]{bricks_collect()}}.
}
\details{
The Databricks Connect path (\code{method = "databricks_connect"}) needs a
one-time backend install (\code{\link[pysparklyr:install_databricks]{pysparklyr::install_databricks()}}) and a token
with cluster/compute scope; a SQL-warehouse-scoped token works for
\code{\link[=databricks_connect]{databricks_connect()}} but not here. Credentials are read from the
environment (\code{DATABRICKS_HOST}, \code{DATABRICKS_TOKEN}).
}
\examples{
\dontrun{
sc <- databricks_spark_connect(method = "databricks_connect", serverless = TRUE)
sparklyr::spark_disconnect(sc)
}
}
\seealso{
\code{\link[=bricks_connect]{bricks_connect()}}, \code{\link[=databricks_connect]{databricks_connect()}}
}
