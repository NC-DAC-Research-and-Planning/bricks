% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tbl.R
\name{bricks_tbl}
\alias{bricks_tbl}
\title{Reference a Databricks table lazily}
\usage{
bricks_tbl(con, table)
}
\arguments{
\item{con}{A \code{bricks_con} from \code{\link[=bricks_connect]{bricks_connect()}}.}

\item{table}{A table identifier: \code{"catalog.schema.table"} (also accepts
\code{"schema.table"} or \code{"table"}). Ignored on the \code{"csv"} backend, which
returns the extract.}
}
\value{
A lazy \code{tbl}, or a local \link[tibble:tibble]{tibble} for the \code{"csv"}
backend.
}
\description{
Returns a lazy dplyr/dbplyr (or sparklyr) table. Pipe further dplyr verbs
onto it and they push down to Databricks compute; nothing is pulled locally
until \code{\link[=bricks_collect]{bricks_collect()}}.
}
\examples{
\dontrun{
con <- bricks_connect()
bricks_tbl(con, "main.default.diamonds") |>
  dplyr::filter(cut == "Ideal") |>
  bricks_collect()
}
}
\seealso{
\code{\link[=bricks_sql]{bricks_sql()}}, \code{\link[=bricks_collect]{bricks_collect()}}
}
