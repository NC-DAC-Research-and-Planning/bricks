% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{databricks_connect}
\alias{databricks_connect}
\title{Connect to a Databricks SQL warehouse (DBI/odbc)}
\usage{
databricks_connect(warehouse_id = Sys.getenv("DATABRICKS_WAREHOUSE_ID"))
}
\arguments{
\item{warehouse_id}{SQL warehouse id. Defaults to the
\code{DATABRICKS_WAREHOUSE_ID} environment variable.}
}
\value{
A \link[DBI:DBI-package]{DBI} connection object.
}
\description{
Opens a \link[DBI:DBI-package]{DBI} connection to a Databricks SQL warehouse
through the odbc driver. Computation happens locally, after rows are pulled.
}
\details{
Configuration is read from the environment (for example \verb{~/.Renviron}):
\itemize{
\item \code{DATABRICKS_HOST} --- the workspace URL, e.g. \verb{https://adb-...}.
\item \code{DATABRICKS_WAREHOUSE_ID} --- the SQL warehouse id.
}

Authentication is resolved by \code{\link[odbc:databricks]{odbc::databricks()}}: a \code{DATABRICKS_TOKEN}
personal access token when set, otherwise the OAuth token from the matching
Databricks CLI profile.
}
\examples{
\dontrun{
con <- databricks_connect()
DBI::dbGetQuery(con, "select 1")
DBI::dbDisconnect(con)
}
}
\seealso{
\code{\link[=bricks_connect]{bricks_connect()}}, \code{\link[=databricks_spark_connect]{databricks_spark_connect()}}
}
