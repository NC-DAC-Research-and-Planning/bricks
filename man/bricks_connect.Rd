% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{bricks_connect}
\alias{bricks_connect}
\title{Open a backend-agnostic Databricks connection}
\usage{
bricks_connect(backend = bricks_backend(), path = NULL, ...)
}
\arguments{
\item{backend}{One of \code{"odbc"}, \code{"databricks"}, \code{"databricks_connect"}, or
\code{"csv"}. Defaults to \code{\link[=bricks_backend]{bricks_backend()}}.}

\item{path}{Path to a CSV extract. Required when \code{backend = "csv"}, ignored
otherwise.}

\item{...}{Passed to the underlying connector, e.g. \code{cluster_id} or
\code{serverless} for the Spark backends (see \code{\link[=databricks_spark_connect]{databricks_spark_connect()}}).}
}
\value{
A \code{bricks_con} object.
}
\description{
Opens a connection using the resolved \code{\link[=bricks_backend]{bricks_backend()}} (or an explicit
\code{backend}) and wraps it in a lightweight \code{bricks_con} object that the other
\verb{bricks_*} verbs dispatch on. Pair every connection with
\code{\link[=bricks_disconnect]{bricks_disconnect()}}, typically via \code{\link[=on.exit]{on.exit()}}.
}
\examples{
# A CSV-backed connection needs no live workspace:
con <- bricks_connect("csv", path = "extract.csv")
con

\dontrun{
con <- bricks_connect()
on.exit(bricks_disconnect(con), add = TRUE)
bricks_sql(con, "select 1 as x") |> bricks_collect()
}
}
\seealso{
\code{\link[=bricks_tbl]{bricks_tbl()}}, \code{\link[=bricks_sql]{bricks_sql()}}, \code{\link[=bricks_collect]{bricks_collect()}},
\code{\link[=bricks_disconnect]{bricks_disconnect()}}
}
